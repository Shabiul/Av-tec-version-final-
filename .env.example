# Google Apps Script Web App URL used by the /api/quote route to append
# enquiries as rows to a Google Sheet. See README for setup steps.
# Copy this file to `.env.local` and set a real value. Never commit `.env.local`.
GOOGLE_SHEETS_WEBHOOK_URL=
WEB3FORMS_ACCESS_KEY=
